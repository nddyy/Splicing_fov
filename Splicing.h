#ifndef SPLICING
#define SPLICING
#include <vector>
#include <iostream>
#include <string>
#include <thread>
using namespace std;

vector<string> getFiles(string);
int Splicing(string, string);
vector<string> CommandLineDeal(string);
#endif 
